# ARC Studio Elite - منصة معرض الأعمال المعماري

منصة ويب متطورة وتفاعلية (Single Page Application) مخصصة لعرض المحافظ المعمارية (Portfolios). يتميز المشروع بنظام إدارة محتوى داخلي (CMS)، دعم كامل لتعدد اللغات (العربية، الإنجليزية، والتركية)، وتكامل مع الذكاء الاصطناعي لتوليد النصوص والمفاهيم.

---

## 🛠 التقنيات المستخدمة (Tech Stack)

تم بناء هذا المشروع باستخدام أحدث تقنيات الويب الحديثة دون الحاجة لتعقيدات أدوات البناء التقليدية (No-Build Setup)، معتمداً على وحدات ES Modules:

*   **المكتبة الأساسية:** React 19 (عبر ESM).
*   **اللغة البرمجية:** TypeScript (TSX).
*   **التصميم والتنسيق:** Tailwind CSS (عبر CDN).
*   **الرسوم المتحركة:** Framer Motion.
*   **الأيقونات:** Lucide React.
*   **الذكاء الاصطناعي:** Google Gemini API (`@google/genai`).
*   **تخزين البيانات:** LocalStorage (قاعدة بيانات محلية داخل المتصفح).

---

## 📂 هيكلية المشروع (Project Structure)

تم تنظيم الملفات بطريقة معيارية لسهولة الصيانة والتطوير:

### 1. الملفات الجذرية (Root)
*   `index.html`: نقطة الانطلاق للمشروع. يحتوي على استدعاء المكتبات الخارجية عبر `importmap` وإعدادات التحميل الأولية.
*   `index.tsx`: المحرك الرئيسي للتطبيق. يدير التوجيه (Routing) وتجميع المكونات الأساسية.
*   `translations.ts`: ملف القاموس اللغوي. يحتوي على كافة النصوص المترجمة (EN, AR, TR).
*   `metadata.json`: ملف إعدادات الصلاحيات (مثل الكاميرا أو الموقع الجغرافي).

### 2. المجلد `contexts/` (إدارة الحالة)
*   `language-context.tsx`: يدير لغة الموقع الحالية واتجاه الصفحة (RTL/LTR).
*   `data.ts`: يحتوي على البيانات الافتراضية للمشاريع (يستخدم كنسخة احتياطية عند خلو التخزين المحلي).

### 3. المجلد `app/` (المكونات والميزات)
*   `Header.tsx`: شريط التنقل العلوي المتجاوب، يحتوي على مبدل اللغات.
*   `Admin.tsx`: لوحة التحكم الكاملة. تسمح بإضافة/تعديل/حذف المشاريع، وتعديل بيانات "من نحن" والتواصل، وتوليد النصوص بالذكاء الاصطناعي.
*   `AiGenerator.tsx`: أداة للزوار لتوليد مفاهيم معمارية بناءً على أوصاف نصية باستخدام Gemini AI.
*   `About.tsx`: صفحة السيرة الذاتية، الفلسفة المعمارية، والمهارات.
*   `Contact.tsx`: صفحة التواصل ونموذج إرسال البريد الإلكتروني.

### 4. الصفحات الرئيسية
*   `Home.tsx`: الصفحة الرئيسية (Hero Section) مع الرسوم المتحركة الافتتاحية.
*   `Projects.tsx`: شبكة عرض المشاريع مع نظام التصفية (Filtering) وعرض التفاصيل (Modal).

---

## ⚙️ المواصفات والمميزات

1.  **نظام إدارة المحتوى (CMS):**
    *   يمكنك إضافة مشاريع جديدة، صور، مخططات، وروابط فيديو دون كتابة أي كود.
    *   يتم حفظ التغييرات فوراً في متصفح المستخدم (LocalStorage).

2.  **تعدد اللغات (i18n):**
    *   دعم كامل للغة العربية مع قلب التصميم تلقائياً (RTL).
    *   سهولة إضافة لغات جديدة عبر ملف `translations.ts`.

3.  **الذكاء الاصطناعي (AI Powered):**
    *   توليد وصف للمشاريع وسير ذاتية احترافية بضغطة زر داخل لوحة التحكم.
    *   أداة للزوار لاستلهام أفكار معمارية.

4.  **الأداء والتصميم:**
    *   تصميم متجاوب بالكامل (Mobile-First).
    *   مؤشر ماوس مخصص (Custom Cursor).
    *   انتقالات سلسة بين الصفحات (Page Transitions).

---

## 🔐 كيفية تغيير رمز الحماية (PIN) للوحة التحكم

لوحة التحكم (`/admin`) محمية برمز PIN بسيط لمنع الوصول غير المصرح به من الزوار العاديين (Client-side protection).

**لتغيير الرمز الافتراضي (2025)، اتبع الخطوات التالية:**

1.  افتح الملف: `app/Admin.tsx`.
2.  ابحث عن السطر البرمجي التالي (تقريباً في السطر 47):
    ```typescript
    const handleLogin = (e: React.FormEvent) => {
      e.preventDefault();
      // Simple PIN for demonstration
      if (pin === '2025') {  // <--- قم بتغيير الرقم هنا
        setIsAuthenticated(true);
      } else {
    ```
3.  استبدل الرقم `'2025'` بالرمز الجديد الذي تريده (مثلاً `'1234'` أو أي نص آخر).
4.  احفظ الملف.

---

## 🚀 كيفية التشغيل (How to Run)

بما أن المشروع يعتمد على ES Modules، لا يمكنك فتح ملف `index.html` مباشرة بالنقر عليه. يجب تشغيله عبر "خادم محلي" (Local Server) لتجنب مشاكل CORS.

**الطريقة الأسهل (باستخدام VS Code):**
1.  قم بتثبيت إضافة **Live Server**.
2.  اضغط بزر الماوس الأيمن على `index.html`.
3.  اختر **Open with Live Server**.

**تنبيه هام حول مفاتيح API:**
لعمل ميزات الذكاء الاصطناعي، تأكد من وجود مفتاح API صالح لـ Google Gemini. في البيئة الحالية، يتم الاعتماد على `process.env.API_KEY`، ولكن نظراً لأن هذا تطبيق واجهة أمامية بحت (Frontend Only)، قد تحتاج لتعديل طريقة تمرير المفتاح في ملف `app/AiGenerator.tsx` و `app/Admin.tsx` أو وضعه يدوياً للتجربة.


